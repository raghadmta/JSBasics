const msg = [
    'hi',
    'there',
    'what r u doin',
    'great meeting you'
]

//while with array
let index = 0;
while (index < msg.length) {
    console.log(index, msg[index]);
    index++;
}


// for loop with array
for (let i = 0; i < msg.length; i++) {
    console.log(i, msg[i]);
}

