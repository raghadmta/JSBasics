//Cookies
document.cookie = `email=example@example.com;expires=${new Date(2023, 2, 8).toUTCString()}`; // to add an expiration date to the cookies
//Cookies
document.cookie = `name=raghad;expires=${new Date(2023, 2, 8).toUTCString()}`;
// run document.cookie in the conosle to print the cookies