let age = prompt("how old are you ?");

// condtion ? value if true : value if false;
// age >= 18 ? alert('you can drive !') : alert('you are not allowed to drive');
let reslut = age >= 18 ? 'you can drive !' : 'you are not allowed to drive X';
alert(reslut);